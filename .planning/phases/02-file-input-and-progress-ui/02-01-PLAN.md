---
phase: 02-file-input-and-progress-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/drop-zone.ts
  - src/ui/target-config.ts
  - src/ui/file-validation.ts
  - src/styles/main.css
  - index.html
  - vitest.config.ts
  - tests/drop-zone.test.ts
  - tests/file-validation.test.ts
  - tests/target-config.test.ts
autonomous: true
requirements: [INP-01, INP-02, INP-03, INP-04, INP-05, INP-06]

must_haves:
  truths:
    - "User can drag and drop PDF files onto the drop zone and see them accepted"
    - "User can click the drop zone to browse and select PDF files"
    - "Non-PDF files are rejected with a per-file message naming the rejected file"
    - "User can set a max file size target in MB (default 4)"
    - "User can set a percentage reduction target (default 50%)"
    - "User can toggle between size and percentage modes without losing the other value"
  artifacts:
    - path: "src/ui/drop-zone.ts"
      provides: "Drag-drop zone with click-to-browse fallback"
      exports: ["createDropZone"]
    - path: "src/ui/file-validation.ts"
      provides: "PDF magic bytes validation"
      exports: ["validateFiles", "isPdf"]
    - path: "src/ui/target-config.ts"
      provides: "Size/percentage toggle and input fields"
      exports: ["createTargetConfig"]
    - path: "src/styles/main.css"
      provides: "All UI styles"
    - path: "tests/drop-zone.test.ts"
      provides: "Drop zone unit tests"
    - path: "tests/file-validation.test.ts"
      provides: "File validation unit tests"
    - path: "tests/target-config.test.ts"
      provides: "Target config unit tests"
  key_links:
    - from: "src/ui/drop-zone.ts"
      to: "src/ui/file-validation.ts"
      via: "import validateFiles"
      pattern: "validateFiles"
    - from: "src/ui/target-config.ts"
      to: "src/compression/types.ts"
      via: "import CompressionTarget"
      pattern: "CompressionTarget"
---

<objective>
Build the file input UI (drag-drop zone, click-to-browse, PDF validation) and compression target configuration (size/percentage toggle) with full test coverage.

Purpose: Give users a way to provide PDF files and configure compression targets -- the two inputs needed before compression can begin.
Output: Three UI modules (`drop-zone.ts`, `file-validation.ts`, `target-config.ts`), styles, updated `index.html` shell, and unit tests for all three.
</objective>

<execution_context>
@C:/Users/abdie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-input-and-progress-ui/02-RESEARCH.md

<interfaces>
<!-- Key types from Phase 1 that this plan's modules will use -->

From src/compression/types.ts:
```typescript
export type CompressionTarget =
  | { mode: 'size'; maxBytes: number }
  | { mode: 'percentage'; reductionPct: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test infrastructure and file validation module</name>
  <files>vitest.config.ts, src/ui/file-validation.ts, tests/file-validation.test.ts, tests/drop-zone.test.ts, tests/target-config.test.ts</files>
  <action>
1. Install jsdom as dev dependency: `npm install -D jsdom`

2. Update `vitest.config.ts`: add a test environment override so files in `tests/` that test DOM modules use jsdom. Use the `environmentMatchGlobs` option:
   ```
   test: {
     environmentMatchGlobs: [
       ['tests/drop-zone.test.ts', 'jsdom'],
       ['tests/file-validation.test.ts', 'jsdom'],
       ['tests/target-config.test.ts', 'jsdom'],
       ['tests/progress.test.ts', 'jsdom'],
       ['tests/app.test.ts', 'jsdom'],
     ],
     // keep existing testTimeout, pool, poolOptions
   }
   ```

3. Create `src/ui/file-validation.ts` with:
   - `isPdf(file: File): Promise<boolean>` -- reads first 5 bytes, checks for `%PDF-` magic bytes (0x25, 0x50, 0x44, 0x46, 0x2D). Also accept if `file.type === 'application/pdf'` as fast path, but always fall back to magic bytes if MIME is empty or generic.
   - `ValidationResult` interface: `{ valid: File[]; rejected: Array<{ file: File; reason: string }> }`
   - `validateFiles(files: File[]): Promise<ValidationResult>` -- iterates files, calls isPdf on each, returns categorized results. Rejection reason: `"${file.name}" is not a PDF file`.

4. Create `tests/file-validation.test.ts`:
   - Test: PDF with correct magic bytes is accepted
   - Test: File with wrong magic bytes is rejected with message containing filename
   - Test: File with application/pdf MIME but wrong bytes -- should still be rejected (magic bytes are authoritative)
   - Test: Empty file is rejected
   - Test: Multiple files returns correct valid/rejected split
   Use `new File([new Uint8Array([0x25, 0x50, 0x44, 0x46, 0x2D, ...rest])], 'test.pdf', { type: 'application/pdf' })` to construct test files.

5. Create test scaffolds for Wave 0 coverage (failing tests that Plan 02 will make pass):
   - `tests/drop-zone.test.ts`: Scaffold with `describe('drop-zone')` and skipped tests for drag-and-drop callback, click-to-browse callback, and rejected file display. Import will fail until drop-zone.ts exists -- use `describe.skip` or dynamic import guard.
   - `tests/target-config.test.ts`: Scaffold with `describe('target-config')` and skipped tests for size mode default, percentage mode default, toggle preserves values. Same import guard pattern.

Actually -- do NOT skip tests. Write real tests for drop-zone and target-config that will pass once Task 2 creates the modules. Use dynamic imports in the test setup so the test file is valid even before the module exists. Pattern:
```typescript
let createDropZone: typeof import('../src/ui/drop-zone').createDropZone;
beforeAll(async () => {
  const mod = await import('../src/ui/drop-zone');
  createDropZone = mod.createDropZone;
});
```

For drop-zone tests (INP-01, INP-02):
- "drag and drop": Create a container div, call createDropZone(container, onFiles). Simulate a drop event with `new Event('drop')` and set `dataTransfer.files`. Verify onFiles was called with the files.
- "click to browse": After createDropZone, find the hidden input element, dispatch a change event with files. Verify onFiles called.
- "rejected files shown": Drop a mix of PDF and non-PDF files. Verify rejection messages appear in the DOM.

For target-config tests (INP-04, INP-05, INP-06):
- "size mode": createTargetConfig returns getTarget() that defaults to `{ mode: 'size', maxBytes: 4194304 }` (4MB).
- "percentage mode": After toggling mode, getTarget() returns `{ mode: 'percentage', reductionPct: 50 }`.
- "toggle preserves values": Change size to 8MB, toggle to percentage, toggle back, getTarget() still returns 8MB.

Run: `npx vitest run tests/file-validation.test.ts` -- must pass.
Run: `npx vitest run tests/drop-zone.test.ts tests/target-config.test.ts` -- these will fail (modules not yet created). That is expected and correct for Wave 0.
  </action>
  <verify>
    <automated>npx vitest run tests/file-validation.test.ts --reporter=verbose</automated>
  </verify>
  <done>file-validation.ts passes all tests. jsdom configured. drop-zone and target-config test files exist with real tests (will pass after Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop zone, target config, styles, and HTML shell</name>
  <files>src/ui/drop-zone.ts, src/ui/target-config.ts, src/styles/main.css, index.html</files>
  <behavior>
    - createDropZone(container, onFiles): renders drop zone div with drag-over highlighting, calls onFiles with valid PDFs, shows rejection messages for non-PDFs
    - createTargetConfig(container): renders toggle + input fields, getTarget() returns correct CompressionTarget
    - Drop zone tests from Task 1 pass
    - Target config tests from Task 1 pass
  </behavior>
  <action>
1. Create `src/ui/drop-zone.ts`:
   - Export `createDropZone(container: HTMLElement, onFiles: (files: File[]) => void): { reset: () => void }`
   - Build drop zone DOM: outer div.drop-zone containing a prompt paragraph ("Drop PDF files here or [browse]") and a hidden `<input type="file" accept=".pdf,application/pdf" multiple>`.
   - Counter-based drag flicker prevention (increment on dragenter, decrement on dragleave, reset on drop). Add/remove `drag-over` class.
   - `dragover` handler: `e.preventDefault()` + `e.dataTransfer.dropEffect = 'copy'` (CRITICAL -- without preventDefault, drop event never fires).
   - `drop` handler: extract files from `e.dataTransfer.files`, call `validateFiles()` from file-validation.ts. Call `onFiles(result.valid)` if any valid. For rejected files, render rejection messages in a `.drop-zone__errors` div below the prompt (each message: `"filename.ext" is not a PDF file`). Clear errors on next drop/select.
   - Click handler on the browse button: `e.stopPropagation()` then `fileInput.click()`. Also make the entire drop zone clickable (click on zone triggers fileInput.click()).
   - `change` handler on fileInput: same validate-then-callback flow. Reset `fileInput.value = ''` after processing.
   - Window-level `dragover` and `drop` prevention: `window.addEventListener('dragover/drop', e => e.preventDefault())` to stop browser from opening dropped files.
   - The `reset()` return function clears error messages and any file state (used by Phase 3 restart).

2. Create `src/ui/target-config.ts`:
   - Export `createTargetConfig(container: HTMLElement): { getTarget: () => CompressionTarget }`
   - Import `CompressionTarget` from `../../src/compression/types` (use relative path from src/ui/).
   - Internal state: `{ mode: 'size' | 'percentage', sizeValueMB: 4, percentValue: 50 }`.
   - Render: a `.target-config` div containing:
     - A two-button toggle (Size / Percentage) -- the active mode button gets class `active`.
     - A `.target-config__input` div with a number input. In size mode: label "Max file size", input value=4, suffix "MB", min=0.1, step=0.1. In percentage mode: label "Reduce by", input value=50, suffix "%", min=1, max=99, step=1.
   - Toggle click: update mode, swap input field (preserve both values in state -- changing the input only updates the active mode's value).
   - `getTarget()`: returns `{ mode: 'size', maxBytes: sizeValueMB * 1024 * 1024 }` or `{ mode: 'percentage', reductionPct: percentValue }`.
   - Input validation: clamp on blur -- size min 0.1, percentage 1-99. On invalid input (NaN), reset to default.

3. Create `src/styles/main.css` with clean, minimal dev-tool aesthetic:
   - System font stack (`-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif`).
   - Dark background (#1a1a2e or similar dark navy), light text (#e0e0e0).
   - `.drop-zone`: dashed border (#444), rounded corners, large padding (60px+), centered text, cursor pointer. Hover/drag-over state: border color brightens (#888), subtle background shift.
   - `.drop-zone.drag-over`: solid border, slightly brighter background to indicate active drop target.
   - `.drop-zone__browse`: inline text button styled as link (underline, accent color like #6c8aff).
   - `.drop-zone__errors`: red-tinted text (#ff6b6b), small font, margin-top.
   - `.target-config`: flex row, gap, items centered.
   - `.target-config button`: pill-shaped, border, no fill. `.active`: filled with accent color.
   - `.target-config input`: number input, small width (80px), dark bg, light text, border.
   - Body: margin 0, min-height 100vh, display flex, flex-direction column, align-items center, padding-top ~80px.
   - `#app`: max-width 600px, width 100%, padding 0 20px.
   - Keep it minimal -- no animations, no gradients, no box-shadows heavier than 1px.

4. Update `index.html`:
   - Keep the existing `<meta>` tags and script module src.
   - Add `<link rel="stylesheet" href="/src/styles/main.css">` in head.
   - Change `<div id="app">` to just be an empty container (remove "PDF Compress - Loading Engine..." text -- the app.ts orchestrator in Plan 02 will populate it).
   - Add a simple `<h1>` above `#app`: "PDF Compress" with a `<small>` subtitle "Files never leave your browser".

5. Run all tests: `npx vitest run tests/drop-zone.test.ts tests/target-config.test.ts tests/file-validation.test.ts --reporter=verbose` -- all must pass.
  </action>
  <verify>
    <automated>npx vitest run tests/drop-zone.test.ts tests/target-config.test.ts tests/file-validation.test.ts --reporter=verbose</automated>
  </verify>
  <done>Drop zone accepts PDFs via drag-drop and click-browse, rejects non-PDFs with messages. Target config toggles between size (default 4MB) and percentage (default 50%) modes, preserving values on toggle. All tests green.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass (existing Phase 1 tests + new Phase 2 tests)
- `npx vite build` -- no build errors (all imports resolve)
- Drop zone, target config, and file validation modules export their public APIs
</verification>

<success_criteria>
- 6 requirement IDs covered: INP-01 (drag-drop), INP-02 (click-browse), INP-03 (non-PDF rejection), INP-04 (size target), INP-05 (percentage target), INP-06 (toggle)
- All 3 test files pass with jsdom environment
- index.html loads styles and has app container ready for Plan 02 orchestrator
- No new npm dependencies besides jsdom (dev)
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-input-and-progress-ui/02-01-SUMMARY.md`
</output>
