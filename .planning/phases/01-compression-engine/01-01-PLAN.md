---
phase: 01-compression-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.worker.json
  - vite.config.ts
  - vitest.config.ts
  - index.html
  - src/main.ts
  - src/compression/types.ts
  - src/compression/worker-client.ts
  - src/worker/compression.worker.ts
  - src/worker/ghostscript.ts
  - tests/worker-init.test.ts
autonomous: true
requirements:
  - ENG-01
  - ENG-02
  - ENG-07

must_haves:
  truths:
    - "Vite 8 dev server starts and serves index.html without errors"
    - "WASM binary loads inside a Web Worker and posts 'ready' message to main thread"
    - "Worker is spawned immediately on page load (eager start for background preload)"
    - "Typed discriminated union message protocol compiles and type-checks"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "@jspawn/ghostscript-wasm"
    - path: "vite.config.ts"
      provides: "Vite 8 config with WASM + worker plugins"
      contains: "vite-plugin-wasm"
    - path: "src/compression/types.ts"
      provides: "WorkerCommand and WorkerEvent discriminated unions"
      exports: ["WorkerCommand", "WorkerEvent", "CompressionTarget"]
    - path: "src/worker/compression.worker.ts"
      provides: "Worker entry point handling init command"
      contains: "onmessage"
    - path: "src/worker/ghostscript.ts"
      provides: "WASM module init with print/printErr capture"
      exports: ["initGhostscript", "getGs", "getStderr"]
    - path: "src/compression/worker-client.ts"
      provides: "Typed postMessage wrapper with Transferable support"
      exports: ["createCompressionWorker", "sendCommand"]
  key_links:
    - from: "src/main.ts"
      to: "src/compression/worker-client.ts"
      via: "createCompressionWorker() call on page load"
      pattern: "createCompressionWorker"
    - from: "src/compression/worker-client.ts"
      to: "src/worker/compression.worker.ts"
      via: "new Worker(new URL(...), { type: 'module' })"
      pattern: "new Worker"
    - from: "src/worker/compression.worker.ts"
      to: "src/worker/ghostscript.ts"
      via: "initGhostscript() on 'init' command"
      pattern: "initGhostscript"
---

<objective>
Scaffold the Vite 8 project, validate WASM-in-Worker loading, and establish the typed message protocol that all subsequent plans build on.

Purpose: This is the highest-risk validation -- Vite 8 + vite-plugin-wasm compatibility is LOW confidence (Vite 8 shipped 2026-03-12 with Rolldown internals). If this breaks, a fallback path is needed before any engine work begins. Also establishes the type contracts and worker shell that Plan 02 fills in.

Output: Working Vite 8 project where a Web Worker loads Ghostscript WASM and reports ready. Typed message protocol. Passing integration test.
</objective>

<execution_context>
@C:/Users/abdie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compression-engine/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite 8 project with WASM plugin and validate worker loading</name>
  <files>package.json, tsconfig.json, tsconfig.worker.json, vite.config.ts, vitest.config.ts, index.html, src/main.ts, src/worker/ghostscript.ts, src/worker/compression.worker.ts</files>
  <action>
Initialize project and validate the critical Vite 8 + WASM risk:

1. Run `npm init -y` then install dependencies:
   - `npm install @jspawn/ghostscript-wasm`
   - `npm install -D typescript vite vite-plugin-wasm vite-plugin-top-level-await vitest @vitest/web-worker`

2. Create `tsconfig.json` for main thread: target ES2022, module ESNext, moduleResolution bundler, lib ["ES2022", "DOM"], strict true, outDir dist, include ["src/**/*"]. Exclude worker files via tsconfig.worker.json reference.

3. Create `tsconfig.worker.json` for worker: extends tsconfig.json but lib ["ES2022", "WebWorker"] (no DOM). Include ["src/worker/**/*", "src/compression/types.ts"].

4. Create `vite.config.ts` exactly as specified in research:
   ```typescript
   import { defineConfig } from 'vite'
   import wasm from 'vite-plugin-wasm'
   import topLevelAwait from 'vite-plugin-top-level-await'

   export default defineConfig({
     plugins: [wasm(), topLevelAwait()],
     worker: {
       plugins: () => [wasm(), topLevelAwait()],
     },
     optimizeDeps: {
       exclude: ['@jspawn/ghostscript-wasm'],
     },
     build: {
       target: 'es2022',
       assetsInlineLimit: 0,
     },
   })
   ```

5. Create `vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import wasm from 'vite-plugin-wasm'
   import topLevelAwait from 'vite-plugin-top-level-await'

   export default defineConfig({
     plugins: [wasm(), topLevelAwait()],
     test: {
       testTimeout: 60000,
       pool: 'forks',
     },
   })
   ```

6. Create `index.html` -- minimal Vite entry point with `<script type="module" src="/src/main.ts"></script>` and a single `<div id="app">Loading...</div>`.

7. Create `src/worker/ghostscript.ts` -- WASM module init with print/printErr capture and locateFile callback, exactly as in research code examples. Export `initGhostscript()`, `getGs()`, `getStderr()`.

8. Create `src/worker/compression.worker.ts` -- worker entry point. Import types. Handle `init` command by calling `initGhostscript()` and posting `{ type: 'ready' }`. Add placeholder `compress` case that posts `file-error` with "not implemented". Use typed `post()` helper with Transferable support.

9. Create `src/main.ts` -- import `createCompressionWorker` (from next task's worker-client.ts -- create a minimal inline version for now). Spawn worker immediately on module load. Send `{ type: 'init' }` command. Listen for `ready` event and log to console. Update `#app` div text to "Ghostscript WASM ready" on ready.

10. Add scripts to package.json: `"dev": "vite"`, `"build": "vite build"`, `"preview": "vite preview"`, `"test": "vitest run"`.

11. CRITICAL VALIDATION: Run `npx vite build` and verify it completes without errors. If vite-plugin-wasm fails with Vite 8, fall back to native `.wasm?init` import syntax per research alternatives. Run `npx vite preview` in background, fetch the page to confirm it serves. Kill the preview server.

If Vite 8 + vite-plugin-wasm fails: Remove the plugin. Use Vite's native WASM support with manual `fetch` + `WebAssembly.instantiateStreaming` in the worker. Document the fallback in the SUMMARY.
  </action>
  <verify>
    <automated>cd C:/Users/abdie/Documents/pdf-compress && npx vite build 2>&1 | tail -20</automated>
  </verify>
  <done>Vite 8 builds without errors. WASM plugin (or fallback) works. Worker file is included in build output. Package.json has all dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create typed message protocol and worker-client with integration test</name>
  <files>src/compression/types.ts, src/compression/worker-client.ts, tests/worker-init.test.ts</files>
  <behavior>
    - Test 1: Worker can be created via createCompressionWorker() without throwing
    - Test 2: Sending 'init' command results in 'ready' event within 30 seconds (WASM download + compile)
    - Test 3: WorkerCommand and WorkerEvent types compile correctly (TypeScript catches invalid messages)
    - Test 4: sendCommand with Transferable detaches the source buffer
  </behavior>
  <action>
1. Create `src/compression/types.ts` with the exact discriminated unions from research:
   - `CompressionTarget`: size mode (maxBytes) | percentage mode (reductionPct)
   - `WorkerCommand`: 'init' | 'compress' (with fileIndex, fileName, buffer, target)
   - `WorkerEvent`: 'ready' | 'progress' | 'file-done' | 'file-skipped' | 'file-error'
   - Export all types. Include `CompressionResult` type: `{ fileIndex: number; fileName: string; originalSize: number; compressedSize: number; buffer: ArrayBuffer; skipped: boolean }`

2. Create `src/compression/worker-client.ts`:
   - `createCompressionWorker()`: returns `new Worker(new URL('../worker/compression.worker.ts', import.meta.url), { type: 'module' })`
   - `sendCommand(worker, cmd, transfer?)`: typed wrapper around `worker.postMessage(cmd, { transfer })`
   - Export both functions.

3. Update `src/main.ts` to import from worker-client.ts instead of inline worker creation. Keep the eager spawn + init + ready listener pattern.

4. Create `tests/worker-init.test.ts`:
   - Import `@vitest/web-worker` at top to enable worker simulation
   - Test that createCompressionWorker returns a Worker instance
   - Test that sending 'init' command and listening for 'ready' event works (this is an integration test -- it loads actual WASM, so set timeout to 60s)
   - Test that sendCommand with a Transferable ArrayBuffer detaches the source (buffer.byteLength becomes 0 after transfer)

5. Run `npx vitest run tests/worker-init.test.ts` and ensure tests pass. If @vitest/web-worker has issues with WASM loading (known friction point from research), adjust: either mock the WASM init for the protocol tests and keep one slow integration test, or use `pool: 'forks'` in vitest config.
  </action>
  <verify>
    <automated>cd C:/Users/abdie/Documents/pdf-compress && npx vitest run tests/worker-init.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass. Types compile. Worker loads WASM and posts 'ready'. Transferable detaches source buffer (ENG-07 verified at protocol level). Worker spawns on page load (ENG-01, ENG-02 verified structurally).</done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds (Vite 8 + WASM validated)
- `npx vitest run` -- all tests green
- `npx tsc --noEmit` -- no type errors
- Worker loads WASM and reports ready (ENG-01)
- Worker spawned eagerly in main.ts (ENG-02)
- Transferable pattern used in sendCommand (ENG-07)
</verification>

<success_criteria>
- Vite 8 project builds and serves correctly with WASM plugin (or documented fallback)
- Web Worker loads Ghostscript WASM and posts 'ready' event
- Typed message protocol (WorkerCommand/WorkerEvent) compiles and type-checks
- Integration test passes: init -> ready round-trip
- Transferable ArrayBuffer transfer verified in test
</success_criteria>

<output>
After completion, create `.planning/phases/01-compression-engine/01-01-SUMMARY.md`
</output>
